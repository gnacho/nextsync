//! Conflict resolver dialog.
//!
//! Fase 5 (Task 5.4): keep local / keep remote / open both sides for
//! conflicted files.

/// Placeholder for the conflict resolver.
pub struct ConflictResolver;

impl ConflictResolver {
    /// Reports whether there is a pending conflict.
    ///
    /// Placeholder: always `false` until Fase 5 lands.
    pub fn has_pending() -> bool {
        false
    }
}
