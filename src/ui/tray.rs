//! StatusNotifier tray.
//!
//! Fase 5 (Task 5.5): ksni blocking tray with Open/Settings/Quit menu and
//! monochrome Lucide glyphs (issue #22).

/// Placeholder for the tray icon.
pub struct Tray;

impl Tray {
    /// Returns the number of menu items.
    ///
    /// Placeholder: always `0` until Fase 5 lands.
    pub fn menu_items() -> usize {
        0
    }
}
