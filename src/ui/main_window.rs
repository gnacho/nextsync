//! Main application window.
//!
//! Fase 5 (Task 5.1): `Adw.ApplicationWindow` with a `NavigationSplitView`
//! (account sidebar + content), status label, progress and header buttons.

/// Placeholder for the main window.
pub struct MainWindow;

impl MainWindow {
    /// Returns the window title.
    ///
    /// Placeholder: constant until Fase 5 lands.
    pub fn title() -> &'static str {
        "NextSync"
    }
}
