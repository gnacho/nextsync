//! Account setup wizard.
//!
//! Fase 5 (Task 5.3): login flow v2 (browser-based) and the first-sync
//! confirmation dialog (PROPFIND Depth 1).

/// Placeholder for the setup wizard.
pub struct Setup;

impl Setup {
    /// Reports whether the wizard is currently shown.
    ///
    /// Placeholder: always `false` until Fase 5 lands.
    pub fn is_open() -> bool {
        false
    }
}
