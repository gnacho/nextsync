//! notify_push protocol client.
//!
//! Fase 4 (Task 4.1): WebSocket push client to trigger remote syncs. The
//! exact WebSocket library is an open question (risk register item 1).

/// Placeholder for the push client.
pub struct Push;

impl Push {
    /// Reports whether the push connection is established.
    ///
    /// Placeholder: always `false` until Fase 4 lands.
    pub fn is_connected() -> bool {
        false
    }
}
