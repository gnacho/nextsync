//! Sync scheduler.
//!
//! Fase 2 (Task 2.2): timers that trigger syncs on the four trigger kinds.

/// Placeholder for the scheduler.
pub struct Scheduler;

impl Scheduler {
    /// Reports whether the scheduler is running.
    ///
    /// Placeholder: always `false` until Fase 2 lands.
    pub fn is_running() -> bool {
        false
    }
}
