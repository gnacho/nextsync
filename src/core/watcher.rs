//! Local filesystem watcher.
//!
//! Fase 3 (Task 3.1): inotify watcher over `notify`, feeding events into a
//! channel.

/// Placeholder for the filesystem watcher.
pub struct Watcher;

impl Watcher {
    /// Reports whether the watcher is active.
    ///
    /// Placeholder: always `false` until Fase 3 lands.
    pub fn is_active() -> bool {
        false
    }
}
