//! One-at-a-time sync permit.
//!
//! Fase 2 (Task 2.2): FIFO semaphore so only one sync runs at a time.

/// Placeholder for the sync permit.
pub struct SyncPermit;

impl SyncPermit {
    /// Reports whether a permit is available.
    ///
    /// Placeholder: always `true` until Fase 2 lands.
    pub fn available() -> bool {
        true
    }
}
